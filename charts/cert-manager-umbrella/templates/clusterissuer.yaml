---
{{- with .Values.issuer }}
{{- $resourceName := include "robustName" .Release.Name -}}
apiVersion: cert-manager.io/v1 #cert-manager.io/v1alpha2
kind: ClusterIssuer
metadata:
  name: {{ $resourceName }}-cluster-issuer
spec:
  acme:
    # You must replace this email address with your own.
    # Let's Encrypt will use this to contact you about expiring
    # certificates, and issues related to your account.
    email: {{ .Values.issuer.spec.acme.email }}
    server: {{ .Values.issuer.spec.acme.server }}
    privateKeySecretRef:
      # Secret resource that will be used to store the account's private key.
      name: {{ $resourceName }}-key
    # Add a single challenge solver, HTTP01 using nginx
    {{- with .Values.issuer.spec.solvers }}
    solvers:
    {{- toYaml . | nindent 6 }}
    {{- end }}

{{- end }}