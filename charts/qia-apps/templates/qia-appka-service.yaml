---
{{- if .Values.qia.apps.appka.enabled }}
apiVersion: v1
kind: Service
metadata:
  labels:
    app: "appka"
  name: "appka"
  namespace: qia-cloud
spec:
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
  selector:
    app: "appka"
{{- end }}